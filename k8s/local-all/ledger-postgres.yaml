apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ledger-postgres-pvc
  namespace: tradinggate-demo
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: ledger-postgres
  namespace: tradinggate-demo
spec:
  selector:
    app: ledger-postgres
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ledger-postgres
  namespace: tradinggate-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ledger-postgres
  template:
    metadata:
      labels:
        app: ledger-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: ledger_db
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: tradinggate-app-secrets
                  key: LEDGER_DB_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: tradinggate-app-secrets
                  key: LEDGER_DB_PASSWORD
          volumeMounts:
            - name: ledger-postgres-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: ledger-postgres-data
          persistentVolumeClaim:
            claimName: ledger-postgres-pvc
